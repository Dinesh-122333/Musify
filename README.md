# Musify
A music listening application which has the user favorite playlist and AI driven app to
## 📅 Day 1 — Client Setup

### 🚀 Create the Client Side (React)

Follow the steps below to set up the client side using **React**:

```bash
npx create-react-app client
cd client
```

## 🔧 Update Node Version (18 → 25)

Make sure you're using the latest Node version.

### 🔍 Check Current Node Version
```bash
node -v
nvm install node
nvm use <version>
```
